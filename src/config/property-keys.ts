/**
 * Logger property keys
 */
export const logger = {
  level: "logger.level"
};

/**
 * Web property keys
 */
export const web = {
  port: "web.port"
};

/**
 * Controller property keys
 */
export const controller = {

  grace_period: "controller.grace_period",
  poll_period: "controller.poll_period"

};
